---
title: STM Specification Overview
description: Sonde defines an STM-oriented model for building and evaluating AI-native, AI-friendly CLI behavior in repeatable machine-readable scenarios.
---

## Purpose

Explain Sonde's deterministic model for developing and evaluating AI-native, AI-friendly CLI behavior.

## Inputs

- CLI help/command behavior.
- Scenario metadata captured in manifest.
- Runtime outputs from deterministic command execution.

## Outputs

- Structured observations (stdout, stderr, parsed JSON when available).
- Determinism signals (interactive detection, non-zero exits).
- Scoring-ready run results.

## Key elements

### State transitions

Model deterministic execution stages for each CLI scenario.

### Observations

Capture stdout, stderr, parsed JSON payloads, and prompt interactions.

### Scoring

Compute comparable totals from explicit weighted criteria.

## When to use

Use this spec when you need deterministic test assertions across different CLIs,
environments, or CI runs without relying on free-form manual interpretation.

## Edge cases

- Deterministic checks degrade when interactive prompts are detected.
- Equivalent flags across CLIs may map to different option names and must be captured in manifest options.

## See also

- [Manifest](/docs/foundations/sondage-manifest)
- [CLI Reference](/docs/reference-implementation/cli-reference)
- [Architecture](/docs/reference-implementation/architecture)
