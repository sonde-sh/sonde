{
  "name": "vercel",
  "commands": [
    {
      "name": "deploy-preview",
      "description": "Create a Vercel preview deployment",
      "inputSchema": {
        "type": "object",
        "properties": {
          "projectId": { "type": "string" },
          "gitSha": { "type": "string" }
        },
        "required": ["projectId", "gitSha"],
        "additionalProperties": false
      }
    }
  ]
}
